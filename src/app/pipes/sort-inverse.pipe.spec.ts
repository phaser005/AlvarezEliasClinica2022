import { SortInversePipe } from './sort-inverse.pipe';

describe('SortInversePipe', () => {
  it('create an instance', () => {
    const pipe = new SortInversePipe();
    expect(pipe).toBeTruthy();
  });
});
