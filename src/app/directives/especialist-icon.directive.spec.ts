import { EspecialistIconDirective } from './especialist-icon.directive';

describe('EspecialistIconDirective', () => {
  it('should create an instance', () => {
    const directive = new EspecialistIconDirective();
    expect(directive).toBeTruthy();
  });
});
